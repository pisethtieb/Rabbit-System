rabbitRoutes = FlowRouter.group({
    prefix: '/rabbit'
    //subscriptions: function (params, queryParams) {
    //    // Global subscriptions for route group
    //    this.register('rabbit_address', Meteor.subscribe('rabbit_address'));
    //}
});
